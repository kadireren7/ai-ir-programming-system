"""Backward-compatible re-exports of ``src.*`` (formerly repo-root ``*.py`` shims).

Prefer imports from ``src.…`` in new code. Legacy:

- ``python -m compat.torqa_cli`` — same as ``torqa`` CLI.
- ``python -m compat.kural_parser`` — Kural / IR tooling entry.

"""
