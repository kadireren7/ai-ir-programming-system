# Simulated bad AI output: forgot password in requires (must reject at parse).

module examples.ai_output.rejected

intent bad_omit_password
requires username
result Should never compile

flow:
  create session
