# Intentionally invalid fixtures (P33)

These files exist **only** to prove TORQA’s **hard validation gate**: they must **not** be used as templates or run with `torqa build` expecting success.

- See [`docs/VALIDATION_GATE.md`](../../docs/VALIDATION_GATE.md).
- Runner: `torqa-gate-proof` or `python -m src.benchmarks.gate_proof_cli`.
