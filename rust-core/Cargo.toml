[package]
name = "rust-core"
version = "0.1.0"
edition = "2021"
description = "TORQA reference engine (validation, semantics, execution bridge)"

[lib]
name = "rust_core"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
